<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
    <div class="container">
        <a class="navbar-brand" href="/">{{site}}</a>
        <div class="ms-auto">
            {{#if loggedIn}}
                <div class="d-flex align-items-center">
                    <img src="https://cdn.discordapp.com/avatars/{{discordUser.id}}/{{discordUser.avatar}}.png" 
                         alt="Discord Avatar" 
                         class="rounded-circle me-2" 
                         style="width: 32px; height: 32px;">
                    <span class="text-light me-3">{{discordUser.username}}</span>
                    <a href="/logout" class="btn btn-outline-light btn-sm">
                        <i class="bi bi-box-arrow-right me-1"></i>Logout
                    </a>
                </div>
            {{else}}
                <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#discordAuthModal">
                    <i class="bi bi-discord me-1"></i>Login with Discord
                </button>
            {{/if}}
        </div>
    </div>
</nav>

<!-- Discord Authentication Modal -->
<div class="modal fade" id="discordAuthModal" tabindex="-1" aria-labelledby="discordAuthModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header bg-dark text-white">
                <h5 class="modal-title" id="discordAuthModalLabel">
                    <i class="bi bi-discord me-2"></i>Discord Authentication
                </h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <h6 class="mb-3">Why do we need Discord authentication?</h6>
                <p>To access this site, we need to connect to your Discord account. Here's what we'll access and why:</p>
                <ul class="list-group list-group-flush mb-3">
                    <li class="list-group-item">
                        <i class="bi bi-person-circle text-primary me-2"></i>
                        Basic profile information (username, avatar, etc.) to identify you on the site.
                    </li>
                    <li class="list-group-item">
                        <i class="bi bi-shield-check text-primary me-2"></i>
                        Server membership verification to ensure you're a qualified {{group}} Fleet Commander with the necessary discord roles. Qualified roles are determined by {{group}} Leadership.
                    </li>
                    <li class="list-group-item">
                        <i class="bi bi-chat-dots text-primary me-2"></i>
                        Permission to read and send direct messages, allowing us to communicate important fleet information privately when needed.
                    </li>
                </ul>
                <p class="text-muted small">We value your privacy and only request essential permissions needed for the fleet tool to function.</p>
            </div>
            <div class="modal-footer bg-light">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                <a href="/login" class="btn btn-primary">
                    <i class="bi bi-discord me-1"></i>Proceed with Discord Login
                </a>
            </div>
        </div>
    </div>
</div>